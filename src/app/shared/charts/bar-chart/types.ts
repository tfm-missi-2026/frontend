/**
 * Tipos del `ChartBar`.
 *
 * `BarStackType` define cómo se apilan las series cuando `stacked = true`:
 *  - `"normal"`: valores sumados.
 *  - `"100%"`: porcentaje apilado (todas las barras suman 100).
 */

export type BarStackType = "normal" | "100%";
