/**
 * Public API del barrel de inputs del design system.
 *
 * - `UiInputComponent`: input base con label, hint, error, iconos y
 *   soporte para los tipos nativos (text, email, password, number,
 *   date, time, etc.).
 * - `UiCheckboxComponent`: checkbox accesible del design system.
 */
export { UiInputComponent } from './input/input';
export { UiCheckboxComponent } from './checkbox/checkbox';
