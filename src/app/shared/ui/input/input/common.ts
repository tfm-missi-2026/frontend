import { TypographyType } from '@styles/types/typography';

/**
 * Constantes comunes para los inputs del design system.
 * Réplica de `./common.ts` del proyecto React.
 */

/** Tamaño de tipografía aplicado al `<input>` (mismo que el label `bodyS`). */
export const typographySize: TypographyType = 'bodyS';

/** Altura estándar del input (px). */
export const inputHeight = '40px';

/** Padding horizontal estándar (px) cuando no hay icono en ese lado. */
export const horizontalPaddingNumber = 12;
