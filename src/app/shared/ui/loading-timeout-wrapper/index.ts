/**
 * Public API del `UiLoadingTimeoutWrapper`.
 *
 * Contenedor presentacional que envuelve un botón (o `UiButton`)
 * mostrando un spinner de carga con timeout configurable. Pensado
 * para deshabilitar el botón durante peticiones async y mostrar
 * feedback visual al usuario.
 */
export { UiLoadingTimeoutWrapperComponent } from './loading-timeout-wrapper';
