/**
 * Public API del `UiTextArea`.
 *
 * Textarea multilínea del design system con label, hint, error,
 * `disabled`/`readOnly`, `maxLength` con contador, `rows` configurable
 * y `ControlValueAccessor` para integración con formularios reactivos.
 * Standalone + OnPush + signal APIs.
 */
export { UiTextAreaComponent } from "./text-area.component";
