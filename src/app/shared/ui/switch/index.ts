/**
 * Public API del `UiSwitch`.
 *
 * Toggle / switch del design system con color `brand` o `gray`, label
 * opcional, estado `disabled` y `checkedChange` que emite el nuevo
 * estado. Standalone + OnPush + signal APIs.
 */
export { UiSwitchComponent } from "./switch.component";
export type { SwitchColor } from "./switch.component";
