/**
 * Public API del `UiHeader`.
 *
 * Encabezado semántico (h1–h5) con tipografía y color del design
 * system. Soporta override del tag (`as`) para ajustar la jerarquía
 * visual sin cambiar la semántica.
 */
export { UiHeaderComponent } from "./header.component";
export type { UiHeaderLevel } from "./header.types";
