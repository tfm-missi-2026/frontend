/**
 * Public API del `UiSeparator`.
 *
 * Separador del design system. Renderiza un `<hr>` semántico con
 * control sobre orientación, estilo, grosor, espaciado y color.
 * Standalone + OnPush + signal APIs.
 */
export { UiSeparatorComponent } from './separator';
export type {
  SeparatorProps,
  SeparatorOrientation,
  SeparatorVariant,
  SeparatorThickness,
  SeparatorSpacing,
  SeparatorColor,
} from './separator.types';
