/**
 * Public API del `UiModal`.
 *
 * Modal genérico con backdrop, escape, content projection y botón
 * de cierre opcional. Soporta modo pantalla completa vía
 * `isFullscreen`.
 */
export { UiModalComponent } from './modal';
