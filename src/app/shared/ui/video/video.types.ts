/**
 * Ratios soportados por `UiVideo`.
 *
 * Cada valor se mapea 1:1 a una clase utilitaria de Tailwind
 * (`aspect-square`, `aspect-4/3`, `aspect-video`, `aspect-21/9`).
 */
export type VideoRatio = "1:1" | "4:3" | "16:9" | "21:9";
