/**
 * Public API del `UiTimePicker`.
 *
 * Time picker construido sobre `flatpickr` con label, error, formato
 * configurable, modo 12/24 h, `disabled`/`readOnly` y
 * `ControlValueAccessor` para integración con formularios reactivos.
 *
 * Standalone + OnPush + signal APIs.
 */
export { UiTimePickerComponent } from "./time-picker.component";
