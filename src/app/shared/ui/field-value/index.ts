/**
 * Public API del `UiFieldValue`.
 *
 * Renderiza el valor actual de un campo de formulario o, en su defecto,
 * un placeholder. Diseñado para usarse como capa de texto controlada
 * dentro de inputs y selects que proyectan un valor visible encima del
 * control real (modo display).
 */
export { UiFieldValueComponent } from "./field-value";
