/**
 * Public API del `UiTooltip`.
 *
 * Tooltip con placement configurable, variants y soporte para
 * contenido rico. Se aplica vía componente o directiva.
 */
export { UiTooltipComponent } from './tooltip';
export type {
  TooltipVariant,
  TooltipSide,
  TooltipAlign,
  TooltipVariantType,
  TooltipBaseProps,
} from './tooltip.types';
