/**
 * Public API del `UiRadio`.
 *
 * Radio button accesible del design system con tamaños `md` y `sm`,
 * estado `disabled` y `valueChange` que emite el `value` configurado.
 * Standalone + OnPush + signal APIs.
 */
export { UiRadioComponent } from "./radio.component";
