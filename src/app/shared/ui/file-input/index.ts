/**
 * Public API del `UiFileInput`.
 *
 * Input de archivo del design system con `multiple`, `accept`,
 * `disabled`, `valueChange` que emite `File | File[] | null` y
 * `ControlValueAccessor` para integración con formularios reactivos.
 * Standalone + OnPush + signal APIs.
 */
export { UiFileInputComponent } from "./file-input.component";
