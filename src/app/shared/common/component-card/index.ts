/**
 * Public API del `CommonComponentCard`.
 *
 * Wrapper del namespace `common/` sobre `UiCard` del design system.
 * Standalone + OnPush + signal APIs.
 */
export { CommonComponentCardComponent } from './component-card.component';
