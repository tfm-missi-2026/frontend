/**
 * Public API del `CommonThemeToggle`.
 *
 * Botón del namespace `common/` para alternar entre tema claro y
 * oscuro. Standalone + OnPush + signal APIs.
 */
export { CommonThemeToggleComponent } from './theme-toggle.component';
export type {
  CommonThemeToggleVariant,
  CommonThemeToggleSize,
} from './types';
