/**
 * Variantes visuales soportadas por `UiAlert`.
 * Determinan el color de fondo, el color del icono y el icono por defecto.
 */
export type AlertVariant = 'success' | 'error' | 'warning' | 'info';
